<h1>The Boards Index! </h1>

<ul class="boards">
  <% boards.forEach(function (board) { %>
    <li>
      <a href="#/boards/<%= board.id %>">
        <%= board.escape('title') %>
      </a>
    </li>
  <% }) %>

  <br>
</ul>

<h2>Let's create a new board!</h2>
<div class="new-board"></div>